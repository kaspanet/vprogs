[package]
edition = "2021"
name    = "vprogs-zk-smt-v2"
version = "0.1.0"

[features]
default = ["std"]
host    = ["std"]
std     = []

[dependencies]
blake3 = { version = "1", default-features = false }

[dev-dependencies]
blake3 = "1"
