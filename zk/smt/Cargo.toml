[package]
edition = "2021"
name    = "vprogs-zk-smt"
version = "0.1.0"

[features]
default = ["std"]
host    = ["std"]
std     = []

[dependencies]
blake3 = { version = "1", default-features = false }
borsh  = { version = "1", default-features = false, features = ["derive"] }
