[package]
edition = "2021"
name    = "vprogs-zk-stitcher-guest"
version = "0.1.0"

[dependencies]
borsh           = { version = "1.6.0", features = ["derive"] }
risc0-zkvm      = { version = "3.0", default-features = false }
vprogs-zk-types = { path = "../types", default-features = false }
